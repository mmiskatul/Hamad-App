import { ServerSettingsScreen } from '@/features/settings';

/*
 * "/server-settings" — auth-side binding for the dev-tool Server Settings
 * screen. Reachable from /login when a request fails before sign-in.
 */
export default ServerSettingsScreen;