import { UsageDashboardScreen } from '@/features/dashboard';

/*
 * "/usage" — this period's requests, tokens and per-model breakdown (Figma
 * 142:496), reached from the profile hub's Usage Dashboard row.
 */
export default UsageDashboardScreen;
