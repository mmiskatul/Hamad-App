import { UpgradePlanScreen } from '@/features/chat';

/*
 * "/upgrade" — the plan comparison (Figma 404:1024). Pushed from the chat home's
 * Upgrade chip, the drawer's plan chip, and the after-2-prompts upsell. The
 * (app) layout presents it as a modal, so it slides over the chat rather than
 * replacing it.
 */
export default UpgradePlanScreen;
