import { ServerSettingsScreen } from '@/features/settings';

/*
 * "/server-settings" — app-side binding for the dev-tool Server Settings
 * screen. Auth subtree has a sibling at the same URL for the pre-sign-in case.
 */
export default ServerSettingsScreen;