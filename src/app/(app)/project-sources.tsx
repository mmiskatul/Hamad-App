import { ProjectSourcesScreen } from '@/features/projects';

/*
 * "/project-sources" — context documents for the project in the store's
 * `editingId` (Figma 168:2002 empty / 170:2091 list). Deep-linked without one,
 * it redirects to /projects.
 */
export default ProjectSourcesScreen;
