import { RenameProjectScreen } from '@/features/projects';

/*
 * "/project-rename" — rename the project in the store's `editingId` (Figma
 * 152:1914). Deep-linked without one, it redirects to /projects.
 */
export default RenameProjectScreen;
