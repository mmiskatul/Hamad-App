import { EditProfileScreen } from '@/features/settings';

/*
 * "/profile-edit" — name / email / photo editor (Figma 142:877), reached from
 * the pencil badge on /profile.
 */
export default EditProfileScreen;
