import { ChatFilesScreen } from '@/features/chat';

/*
 * "/chat-files" — attachments on the open conversation (Figma 184:2882),
 * reached from the chat row menu's "Files in chat".
 */
export default ChatFilesScreen;
