import { MemorySummaryScreen } from '@/features/settings';

/*
 * "/memory-summary" — what the assistant currently remembers (Figma 187:824),
 * pushed from the Memory screen's summary row.
 */
export default MemorySummaryScreen;
