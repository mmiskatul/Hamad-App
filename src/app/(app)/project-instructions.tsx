import { ProjectInstructionsScreen } from '@/features/projects';

/*
 * "/project-instructions" — persona / tone for the project in `editingId`
 * (Figma 152:1951). Deep-linked without one, it redirects to /projects.
 */
export default ProjectInstructionsScreen;
